{
  "name": "@procore/js-sdk-sample-app",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start",
    "serve": "lerna run serve"
  },
  "devDependencies": {
    "lerna": "4.0.0"
  },
  "version": "0.0.0"
}
