/// <reference types="@procore/core-scripts/types/e2e" />

declare namespace Cypress {
  interface Chainable {
    // Add your commands type spec here

    // Example:
    // login(user: User): Chainable;
  }
}
